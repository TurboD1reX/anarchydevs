function about(){
    const about = document.querySelector('.aboutUsContainer');
    about.scrollIntoView({ behavior: 'smooth', block: 'start' });
}
function contact(){
    const form = document.querySelector('.form');
    form.scrollIntoView({ behavior: 'smooth', block: 'start' });
}